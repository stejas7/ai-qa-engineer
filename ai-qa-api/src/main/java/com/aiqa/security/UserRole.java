package com.aiqa.security;

/** M14 coarse-grained roles for the first enterprise authorization layer. */
public enum UserRole {
    PLATFORM_ADMIN,
    COMPANY_ADMIN,
    QA_MANAGER,
    TESTER,
    VIEWER
}
